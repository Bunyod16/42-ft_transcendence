/* eslint-disable prettier/prettier */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["source.boringavatars.com", "cdn.discordapp.com"],
  },
};

module.exports = nextConfig;
